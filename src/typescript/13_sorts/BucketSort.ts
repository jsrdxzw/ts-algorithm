/**
 * 桶排序对数据的要求比较高
 * 首先要知道数据的范围
 * 然后根据范围将数据分到小范围的桶中
 * 每个桶采用快速排序
 * 当桶的数量接近数据量的时候，时间复杂度为O(n)
 */
class BucketSort {
  sort(array: number[], bucketNum = 10) {
    const length = array.length
    
  }
}
